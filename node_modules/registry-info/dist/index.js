'use strict';

var _registryUrl = require('registry-url');

var _registryUrl2 = _interopRequireDefault(_registryUrl);

var _registryAuthToken = require('registry-auth-token');

var _registryAuthToken2 = _interopRequireDefault(_registryAuthToken);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

module.exports = function (scope) {
  var registryUrl = (0, _registryUrl2.default)(scope);
  var authToken = (0, _registryAuthToken2.default)(registryUrl);
  var authorization = authToken ? authToken.type + ' ' + authToken.token : undefined;

  return {
    registryUrl: registryUrl, authToken: authToken, authorization: authorization
  };
};