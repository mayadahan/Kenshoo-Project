'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _semver = require('semver');

exports.default = function (_ref, rangeOrTag) {
  var versions = _ref.versions;
  var _ref$distTags = _ref['dist-tags'];
  var distTags = _ref$distTags === undefined ? {} : _ref$distTags;

  var range = (0, _semver.validRange)(rangeOrTag);
  var version = range ? (0, _semver.maxSatisfying)(Object.keys(versions), range) : distTags[rangeOrTag];

  return versions[version];
};